clr_atla_duel_flags_effect = {
	clr_character_flag = duel_agg
	clr_character_flag = duel_def
	clr_character_flag = first_round
	clr_character_flag = second_round
	clr_character_flag = third_round
	clr_character_flag = fourth_round
	clr_character_flag = wounded_in_duel
	clr_character_flag = maimed_in_duel
	clr_character_flag = agni_agg
	clr_character_flag = agni_def
}

# used for bender vs non-bender duels, where you agreed not to use bending
check_if_breaking_promiss_not_to_bend_effect = {
	if = { 
		limit = {
			has_character_flag = agreed_to_duel_without_bending
		}
		custom_tooltip = {
			text = duel_agreed_to_not_bend_tooltip
		}
		prestige = -100
		event_target:hostile = {
			opinion = {
				modifier = opinion_dishonorable_duel_major
				who = ROOT
				months = 36
			}
		}
	}
}