namespace = SWAMP

character_event = {
	id = SWAMP.1
	hide_window = yes
	is_triggered_only = yes
	
	trigger = {
		religion = the_great_swamp
	}
	
	immediate = {
		if = {
			limit = {
				trait = inbred
			}
			remove_trait = inbred
			add_trait = swamp_inbred
		}
	}
}