offmap_decisions = {

	rc_increase_influence = {
		only_rulers = yes
		button_name = offmap_ek_inf
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
<<<<<<< Updated upstream
			is_offmap_governor = offmap_ek
		}
		
		potential = {
			ROOT = {
				is_offmap_tag = offmap_ek
			}
=======
			always = yes
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			always = yes
>>>>>>> Stashed changes
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
<<<<<<< Updated upstream
				is_offmap_governor = offmap_ek
				check_variable = { which = global_royalAuthority value >= -10 }
=======
				NOT = { is_offmap_governor = offmap_ek }
				check_variable = { which = global_royalAuthority value >= 0 }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
>>>>>>> Stashed changes
			}
		}
		
		effect = {
			FROM = {
				change_variable = { which = global_royalAuthority value = -10 }
				add_offmap_currency = {
					offmap = offmap_ek
					value = 150
				}
			}
<<<<<<< Updated upstream
=======
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
			}		
>>>>>>> Stashed changes
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
<<<<<<< Updated upstream
=======
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
>>>>>>> Stashed changes
		}
	}
	
	rc_increase_dai_li_power = {
		only_playable = yes
		button_name = offmap_ek_inf
		ai_check_interval = 120

<<<<<<< Updated upstream
		potential = {
			is_offmap_tag = offmap_ek
		}

		from_potential = {
			has_dlc = "Jade Dragon"
			mercenary = no
			holy_order = no
			is_landed = yes
		}

		allow = {
			OR = {
				has_landed_title = e_earth_kingdom 
				AND = {
					has_landed_title = k_ba_sing_se
					NOT = { is_title_active = e_earth_kingdom }
				}
			}
			check_variable = { which = global_royalAuthority value >= -10 }
		}
		
		effect = {
			change_variable = { which = global_DaiLiFactRandomGain value = 5 }
			change_variable = { which = global_royalAuthority value = -10 }
		}
=======
		from_potential = {
			has_dlc = "Jade Dragon"
			always = yes
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			always = yes
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }				
				check_variable = { which = global_royalAuthority value >= 0 }
			}
		}
		
		effect = {
			add_character_modifier = {
				name = dai_li_inf_mod
				years = 5
				hidden = yes
				stacking = yes
			}
			change_variable = { which = global_royalAuthority value = -10 }
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					years = 1 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
			}			
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}		
>>>>>>> Stashed changes
	}
	
	rc_increase_admin_power = {
		only_playable = yes
		button_name = offmap_ek_inf
		ai_check_interval = 120

<<<<<<< Updated upstream
		potential = {
			is_offmap_tag = offmap_ek
		}

		from_potential = {
			has_dlc = "Jade Dragon"
			mercenary = no
			holy_order = no
			is_landed = yes
		}

		allow = {
			OR = {
				has_landed_title = e_earth_kingdom 
				AND = {
					has_landed_title = k_ba_sing_se
					NOT = { is_title_active = e_earth_kingdom }
				}
			}
			check_variable = { which = global_royalAuthority value >= -10 }
		}
		
		effect = {
			change_variable = { which = global_AdminFactRandomGain value = 5 }
			change_variable = { which = global_royalAuthority value = -10 }
		}
=======
		from_potential = {
			has_dlc = "Jade Dragon"
			always = yes
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			always = yes
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }				
				check_variable = { which = global_royalAuthority value >= 0 }
			}
		}
		
		effect = {
			add_character_modifier = {
				name = admin_inf_mod
				years = 5
				hidden = yes
				stacking = yes
			}
			change_variable = { which = global_royalAuthority value = -10 }
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					years = 1 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
			}
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}		
>>>>>>> Stashed changes
	}
	
	rc_increase_militarist_power = {
		only_playable = yes
		button_name = offmap_ek_inf
		ai_check_interval = 120

<<<<<<< Updated upstream
		potential = {
			is_offmap_tag = offmap_ek
		}

		from_potential = {
			has_dlc = "Jade Dragon"
			mercenary = no
			holy_order = no
			is_landed = yes
		}

		allow = {
			OR = {
				has_landed_title = e_earth_kingdom 
				AND = {
					has_landed_title = k_ba_sing_se
					NOT = { is_title_active = e_earth_kingdom }
				}
			}
			check_variable = { which = global_royalAuthority value >= -10 }
		}
		
		effect = {
			change_variable = { which = global_MilitaryFactRandomGain value = 5 }
			change_variable = { which = global_royalAuthority value = -10 }
		}
=======
		from_potential = {
			has_dlc = "Jade Dragon"
			always = yes
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			always = yes
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }				
				check_variable = { which = global_royalAuthority value >= 0 }
			}
		}
		
		effect = {
			add_character_modifier = {
				name = mil_inf_mod
				years = 5
				hidden = yes
				stacking = yes
			}
			change_variable = { which = global_royalAuthority value = -10 }
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					years = 1 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
			}
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}		
>>>>>>> Stashed changes
	}
	
	rc_increase_noble_power = {
		only_playable = yes
		button_name = offmap_ek_inf
		ai_check_interval = 120

<<<<<<< Updated upstream
		potential = {
			is_offmap_tag = offmap_ek
		}

		from_potential = {
			has_dlc = "Jade Dragon"
			mercenary = no
			holy_order = no
			is_landed = yes
		}

		allow = {
			OR = {
				has_landed_title = e_earth_kingdom 
				AND = {
					has_landed_title = k_ba_sing_se
					NOT = { is_title_active = e_earth_kingdom }
				}
			}
			check_variable = { which = global_royalAuthority value >= -10 }
		}
		
		effect = {
			change_variable = { which = global_NobleFactRandomGain value = 5 }
			change_variable = { which = global_royalAuthority value = -10 }
		}
=======
		from_potential = {
			has_dlc = "Jade Dragon"
			always = yes
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			always = yes
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }				
				check_variable = { which = global_royalAuthority value >= 0 }				
			}
		}
		
		effect = {
			add_character_modifier = {
				name = noble_inf_mod
				years = 5
				hidden = yes
				stacking = yes
			}
			change_variable = { which = global_royalAuthority value = -10 }
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					years = 1 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
			}			
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}		
>>>>>>> Stashed changes
	}
	
}
			
			
		