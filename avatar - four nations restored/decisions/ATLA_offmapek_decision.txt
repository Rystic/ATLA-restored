offmap_decisions = {

	rc_increase_influence = {
		only_rulers = yes
		button_name = offmap_ek_inf
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			always = yes
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			always = yes
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				check_variable = { which = global_royalAuthority value >= 0 }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
			}
		}
		
		effect = {
			FROM = {
				change_variable = { which = global_royalAuthority value = -10 }
				add_offmap_currency = {
					offmap = offmap_ek
					value = 150
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}
	
	rc_increase_dai_li_power = {
		only_playable = yes
		button_name = offmap_ek_inf
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			always = yes
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			always = yes
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }				
				check_variable = { which = global_royalAuthority value >= 0 }
			}
		}
		
		effect = {
			add_character_modifier = {
				name = dai_li_inf_mod
				years = 5
				hidden = yes
				stacking = yes
			}
			change_variable = { which = global_royalAuthority value = -10 }
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					years = 1 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
			}			
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}		
	}
	
	rc_increase_admin_power = {
		only_playable = yes
		button_name = offmap_ek_inf
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			always = yes
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			always = yes
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }				
				check_variable = { which = global_royalAuthority value >= 0 }
			}
		}
		
		effect = {
			add_character_modifier = {
				name = admin_inf_mod
				years = 5
				hidden = yes
				stacking = yes
			}
			change_variable = { which = global_royalAuthority value = -10 }
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					years = 1 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
			}
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}		
	}
	
	rc_increase_militarist_power = {
		only_playable = yes
		button_name = offmap_ek_inf
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			always = yes
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			always = yes
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }				
				check_variable = { which = global_royalAuthority value >= 0 }
			}
		}
		
		effect = {
			add_character_modifier = {
				name = mil_inf_mod
				years = 5
				hidden = yes
				stacking = yes
			}
			change_variable = { which = global_royalAuthority value = -10 }
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					years = 1 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
			}
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}		
	}
	
	rc_increase_noble_power = {
		only_playable = yes
		button_name = offmap_ek_inf
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			always = yes
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			always = yes
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }				
				check_variable = { which = global_royalAuthority value >= 0 }				
			}
		}
		
		effect = {
			add_character_modifier = {
				name = noble_inf_mod
				years = 5
				hidden = yes
				stacking = yes
			}
			change_variable = { which = global_royalAuthority value = -10 }
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					years = 1 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
			}			
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}		
	}

	rc_dai_li_1 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = daili_faction_absolute
					has_status = daili_faction_parliament
					has_status = daili_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = daili_faction_absolute
					has_status = daili_faction_parliament
					has_status = daili_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = daili_faction_absolute
						has_status = daili_faction_parliament
						has_status = daili_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = daili_1
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCDaiLiFactionPower value = -10 }
				change_variable = { which = global_RCAdminFactionPower value = 5 }
				change_variable = { which = global_RCMilitaryFactionPower value = 5 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}
	
	rc_dai_li_2 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = daili_faction_absolute
					has_status = daili_faction_parliament
					has_status = daili_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = daili_faction_absolute
					has_status = daili_faction_parliament
					has_status = daili_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = daili_faction_absolute
						has_status = daili_faction_parliament
						has_status = daili_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = daili_2
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCDaiLiFactionPower value = -20 }
				change_variable = { which = global_RCAdminFactionPower value = 10 }
				change_variable = { which = global_RCMilitaryFactionPower value = 10 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}
	
	rc_dai_li_3 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = daili_faction_absolute
					has_status = daili_faction_parliament
					has_status = daili_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = daili_faction_absolute
					has_status = daili_faction_parliament
					has_status = daili_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = daili_faction_absolute
						has_status = daili_faction_parliament
						has_status = daili_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = daili_3
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCDaiLiFactionPower value = -10 }
				change_variable = { which = global_RCAdminFactionPower value = 5 }
				change_variable = { which = global_RCMilitaryFactionPower value = 5 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}	
	
	rc_admin_1 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = bureaucratic_faction_absolute
					has_status = bureaucratic_faction_parliament
					has_status = bureaucratic_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = bureaucratic_faction_absolute
					has_status = bureaucratic_faction_parliament
					has_status = bureaucratic_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = bureaucratic_faction_absolute
						has_status = bureaucratic_faction_parliament
						has_status = bureaucratic_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = admin_1
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCDaiLiFactionPower value = 5 }
				change_variable = { which = global_RCAdminFactionPower value = -10 }
				change_variable = { which = global_RCMilitaryFactionPower value = 5 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}	
	
	rc_admin_2 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = bureaucratic_faction_absolute
					has_status = bureaucratic_faction_parliament
					has_status = bureaucratic_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = bureaucratic_faction_absolute
					has_status = bureaucratic_faction_parliament
					has_status = bureaucratic_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = bureaucratic_faction_absolute
						has_status = bureaucratic_faction_parliament
						has_status = bureaucratic_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = admin_2
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCDaiLiFactionPower value = 5 }
				change_variable = { which = global_RCAdminFactionPower value = -10 }
				change_variable = { which = global_RCMilitaryFactionPower value = 5 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}

	rc_admin_3 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = bureaucratic_faction_absolute
					has_status = bureaucratic_faction_parliament
					has_status = bureaucratic_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = bureaucratic_faction_absolute
					has_status = bureaucratic_faction_parliament
					has_status = bureaucratic_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = bureaucratic_faction_absolute
						has_status = bureaucratic_faction_parliament
						has_status = bureaucratic_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = admin_3
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCDaiLiFactionPower value = 5 }
				change_variable = { which = global_RCAdminFactionPower value = -10 }
				change_variable = { which = global_RCMilitaryFactionPower value = 5 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}

	rc_admin_4 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = bureaucratic_faction_absolute
					has_status = bureaucratic_faction_parliament
					has_status = bureaucratic_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = bureaucratic_faction_absolute
					has_status = bureaucratic_faction_parliament
					has_status = bureaucratic_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = bureaucratic_faction_absolute
						has_status = bureaucratic_faction_parliament
						has_status = bureaucratic_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = admin_4
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCDaiLiFactionPower value = 10 }
				change_variable = { which = global_RCAdminFactionPower value = -20 }
				change_variable = { which = global_RCMilitaryFactionPower value = 10 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}

	rc_admin_1 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
					OR = {
						has_status = noble_faction_absolute
						has_status = noble_faction_parliament
						has_status = noble_faction_puppet
					}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
					OR = {
						has_status = noble_faction_absolute
						has_status = noble_faction_parliament
						has_status = noble_faction_puppet
					}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = noble_faction_absolute
						has_status = noble_faction_parliament
						has_status = noble_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = noble_1
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCAdminFactionPower value = 5 }
				change_variable = { which = global_RCNobleFactionPower value = -10 }
				change_variable = { which = global_RCMilitaryFactionPower value = 5 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}	
	
	rc_admin_2 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
					OR = {
						has_status = noble_faction_absolute
						has_status = noble_faction_parliament
						has_status = noble_faction_puppet
					}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = noble_faction_absolute
					has_status = noble_faction_parliament
					has_status = noble_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = noble_faction_absolute
						has_status = noble_faction_parliament
						has_status = noble_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = noble_2
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCAdminFactionPower value = 5 }
				change_variable = { which = global_RCNobleFactionPower value = -10 }
				change_variable = { which = global_RCMilitaryFactionPower value = 5 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}

	rc_admin_3 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
					OR = {
						has_status = noble_faction_absolute
						has_status = noble_faction_parliament
						has_status = noble_faction_puppet
					}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
					OR = {
						has_status = noble_faction_absolute
						has_status = noble_faction_parliament
						has_status = noble_faction_puppet
					}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = noble_faction_absolute
						has_status = noble_faction_parliament
						has_status = noble_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = noble_3
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCAdminFactionPower value = 10 }
				change_variable = { which = global_RCNobleFactionPower value = -20 }
				change_variable = { which = global_RCMilitaryFactionPower value = 10 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}

	rc_military_1 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = military_faction_absolute
					has_status = military_faction_parliament
					has_status = military_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = military_faction_absolute
					has_status = military_faction_parliament
					has_status = military_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = military_faction_absolute
						has_status = military_faction_parliament
						has_status = military_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = military_1
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCAdminFactionPower value = 5 }
				change_variable = { which = global_RCNobleFactionPower value = 5 }
				change_variable = { which = global_RCMilitaryFactionPower value = -10 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}

	rc_military_2 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = military_faction_absolute
					has_status = military_faction_parliament
					has_status = military_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = military_faction_absolute
					has_status = military_faction_parliament
					has_status = military_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = military_faction_absolute
						has_status = military_faction_parliament
						has_status = military_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = military_2
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCAdminFactionPower value = 10 }
				change_variable = { which = global_RCNobleFactionPower value = 10 }
				change_variable = { which = global_RCMilitaryFactionPower value = -20 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}

	rc_military_3 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = military_faction_absolute
					has_status = military_faction_parliament
					has_status = military_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = military_faction_absolute
					has_status = military_faction_parliament
					has_status = military_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = military_faction_absolute
						has_status = military_faction_parliament
						has_status = military_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = military_3
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCAdminFactionPower value = 10 }
				change_variable = { which = global_RCNobleFactionPower value = 10 }
				change_variable = { which = global_RCMilitaryFactionPower value = -20 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}

	rc_military_4 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = military_faction_absolute
					has_status = military_faction_parliament
					has_status = military_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = military_faction_absolute
					has_status = military_faction_parliament
					has_status = military_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = military_faction_absolute
						has_status = military_faction_parliament
						has_status = military_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = military_4
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCAdminFactionPower value = 5 }
				change_variable = { which = global_RCNobleFactionPower value = 5 }
				change_variable = { which = global_RCMilitaryFactionPower value = -10 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}

	rc_military_5 = {
		only_rulers = yes
		button_name = offmap_ek_modifier
		ai_check_interval = 120

		from_potential = {
			has_dlc = "Jade Dragon"
			#always = yes
			offmap_ek = {
				OR = {
					has_status = military_faction_absolute
					has_status = military_faction_parliament
					has_status = military_faction_puppet
				}
			}
		}
		
		potential = {
			#ROOT = {
			#is_offmap_tag = offmap_ek
			#}
			#always = yes
			offmap_ek = {
				OR = {
					has_status = military_faction_absolute
					has_status = military_faction_parliament
					has_status = military_faction_puppet
				}
			}
		}

		allow = {
			show_only_failed_conditions = yes
			FROM = {
				show_scope_change = no
				OR = {
					has_landed_title = e_earth_kingdom 
					AND = {
						has_landed_title = k_ba_sing_se
						NOT = { is_title_active = e_earth_kingdom }
					}
				}
				NOT = { is_offmap_governor = offmap_ek }
				NOT = { has_character_modifier = chinese_grace_send_gift_timer }
				offmap_ek = {
					OR = {
						has_status = military_faction_absolute
						has_status = military_faction_parliament
						has_status = military_faction_puppet
					}
				}
			}
		}
		
		effect = {
			FROM = {
				add_character_modifier = {
					name = military_5
					years = 1
					#hidden = yes
				}
			}
			hidden_effect = {
				add_character_modifier = {
					hidden = yes
					name = chinese_grace_send_gift_timer
					days = 180 #Change chinese_grace_send_gift_allow_tt if this number changes
				} 
				change_variable = { which = global_RCAdminFactionPower value = 15 }
				change_variable = { which = global_RCNobleFactionPower value = 15 }
				change_variable = { which = global_RCMilitaryFactionPower value = -30 }
			}		
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.8
				check_variable = { which = global_royalAuthority value >= 80 }
			}
			modifier = {
				factor = 0.6
				check_variable = { which = global_royalAuthority value >= 60 }
				NOT = { check_variable = { which = global_royalAuthority value >= 80 } }
			}
			modifier = {
				factor = 0.4
				check_variable = { which = global_royalAuthority value >= 40 }
				NOT = { check_variable = { which = global_royalAuthority value >= 60 } }
			}			
			modifier = {
				factor = 0.2
				check_variable = { which = global_royalAuthority value >= 20 }
				NOT = { check_variable = { which = global_royalAuthority value >= 40 } }
			}
			modifier = {
				factor = 0
				check_variable = { which = global_royalAuthority value >= 10 }
				NOT = { check_variable = { which = global_royalAuthority value >= 20 } }
			}			
		}
	}	
}
			
			
		